# Core backend dependencies. Pins are lower bounds (>=) so pip can pick the
# newest build compatible with your Python version (works on 3.11 - 3.14).
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
motor>=3.6.0
pydantic>=2.10.4
pydantic-settings>=2.7.1
python-jose[cryptography]>=3.3.0
bcrypt>=4.2.1
python-multipart>=0.0.20
httpx>=0.28.1
beautifulsoup4>=4.12.3
PyMuPDF>=1.25.1
python-docx>=1.1.2
openai>=1.59.0
anthropic>=0.42.0
python-dotenv>=1.0.1
email-validator>=2.2.0
boto3>=1.35.0

# Optional NLP/data extras (better lemmatisation + pandas CSV parsing) live in
# requirements-nlp.txt. They are NOT required: the extractor falls back to a
# built-in tokenizer/lemmatizer and CSV uses the stdlib if they are absent.
# Install them only on Python <= 3.12:  pip install -r requirements-nlp.txt
boto3
